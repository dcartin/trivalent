[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trivalent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numba>=0.60",
    "numpy>=2.0"
]

[tool.setuptools]
packages = ["trivalent"]