[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trivalent"
version = "0.1.0"
dependencies = [
    "numba",
    "numpy"
]

[tool.setuptools]
packages = ["trivalent"]